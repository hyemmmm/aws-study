# S3 데이터 암호화(SSE) 정리

## 헷갈렸던 점

- “키가 있어야 데이터를 볼 수 있다” →  
  내가 키를 직접 들고 열어야 하는 구조라고 오해했다.
- 책/그림에서 **SSE-S3가 KMS에 포함된 것처럼 표현**되어 있어  
  SSE-S3와 SSE-KMS의 관계가 헷갈렸다.
- SSE-S3와 SSE-KMS의 **실질적인 차이와 선택 기준**이 명확하지 않았다.

## 찾아본 내용

## 권한과 암호화 키의 관계

- **권한(IAM / S3 정책)**  
  → 누가 S3 객체에 **접근 요청(GET/PUT)** 을 할 수 있는지 결정  
  → *출입을 허용하는 역할*

- **암호화 키(SSE / KMS)**  
  → 저장된 **암호화된 데이터를 실제로 복호화할 수 있는지** 결정  
  → *데이터를 여는 열쇠 역할*

### 관계 정리
- 권한 O + 키 O → 읽기 가능
- 권한 O + 키 X → ❌ 읽기 불가
- 권한 X → 요청 자체가 ❌

> 권한은 “들어갈 수 있냐”의 문제이고,  
> 암호화 키는 “들어가서 열 수 있냐”의 문제다.

---


### 1. 데이터 암호화(SSE)의 의미
- SSE(Server-Side Encryption)는  
  **데이터를 ‘보여줄 때’ 암호화하는 것이 아니라  
  ‘저장할 때(at rest)’ 암호화하는 것**이다.
- 사용자는 항상 **평문 데이터**를 업로드/다운로드하고,
- S3가 내부적으로:
  - 저장 시 암호화
  - 조회 시 복호화
  를 대신 수행한다.
- 따라서 **암호화는 접근 제어(IAM)를 대체하지 않으며**,  
  디스크 유출·백업 유출·물리적 사고 등을 대비한 보호 수단이다.

---

### 2. SSE 종류 정리 (정확한 구조)

- **SSE (Server-Side Encryption)**
  - **SSE-S3**
    - S3 자체 키 사용 (KMS 사용 ❌)
  - **SSE-KMS**
    - AWS 관리형 KMS 키 (`aws/s3`)
    - 고객 관리형 KMS 키
  - **SSE-C**
    - 사용자 제공 키 (요청마다 전달)


- **SSE-S3는 KMS를 사용하지 않는다.**
- 책/그림에서 SSE-S3를 KMS 내부에 넣은 것은  
  “AWS가 키를 관리한다”는 개념 설명용인 것 같다. 기술적으로는 부정확하다.

---

### 3. SSE-S3 vs SSE-KMS 차이

#### SSE-S3
- AWS가 키 생성·관리 전부 자동 처리
- KMS 호출 없음
- 비용 없음
- 설정 매우 간단
- 키 사용 이력 감사 불가
- 👉 “기본적인 데이터 보호만 필요할 때”

#### SSE-KMS
- KMS 키 사용
- CloudTrail로 **키 사용 이력 감사 가능**
- KMS 키 정책으로 **접근 통제 가능**
- 비용 및 설정 복잡도 있음
- 👉 “보안, 감사, 컴플라이언스 요구가 있을 때”

---

### 4. SSE-C 특징
- 키를 AWS에 저장하지 않음
- 매 요청마다 키를 직접 전달
- 콘솔/버킷 기본 암호화로 설정 불가
- 운영 난이도 높고 실무에서는 거의 사용하지 않음
- 👉 “AWS도 키를 가지면 안 되는 특수 규제 환경”

---

### 5. SSE 선택 기준

- **SSE-S3**
  - 암호화는 필요하지만 키·정책·감사까지는 필요 없음
  - 운영을 최대한 단순하게 하고 싶을 때
  - 👉 “기본 보호용”

- **SSE-KMS (AWS 관리형 KMS 키)**
  - KMS 기반 암호화와 **감사 로그(CloudTrail)** 는 필요
  - 키 정책/차단까지 직접 관리할 필요는 없음
  - 👉 “감사는 필요하지만 키 통제는 AWS에 맡김”

- **SSE-KMS (고객 관리형 KMS 키)**
  - 키 정책을 직접 관리해야 함
  - 사고 시 **키 Disable로 즉시 전체 차단** 가능
  - 접근 제한(IAM)과 달리 **암호 자체를 무효화하는 비상 스위치 역할**
  - 👉 “보안·규제·사고 대응이 중요한 데이터”

- **SSE-C**
  - AWS에 키를 절대 맡기면 안 되는 특수 규제 환경
  - 운영 난이도 매우 높아 실무에서는 거의 사용하지 않음


